spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  jpa.open-in-view: false

logging:
  level:
    root: INFO
    com.pgmanagement.pgservice: INFO
    org.hibernate: WARN

server:
  error:
    include-stacktrace: never
    include-message: never
    include-exception: false